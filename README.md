# HashBrown
A deterministic password generator called HashBrown Maker that creates reproducible, customizable hashes from a phrase and secret seed. Supports length, allowed characters, and mandatory character types (lower, upper, digit, symbol) for secure, compliant passwords.
